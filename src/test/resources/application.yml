mongo:
  hosts:
    - localhost:27017
  database: test
  retryWrites: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,threadump
  endpoint:
    health:
      show-details: always